services:
  tether:
    image: ghcr.io/sighmonis/tether:latest
    ports:
      - "5225:5225"
    volumes:
      - ./tether-data:/data
    environment:
      - TZ=Australia/Melbourne
      - TETHER_BASE_URL=http://192.168.1.x:5225
    restart: unless-stopped
